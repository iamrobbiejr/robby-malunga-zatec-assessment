/**
 * Albums component
 * Perform all CRUD operations
 */

import React from 'react';

const Albums = (props) => {

    // initialize global variables


    return (
        <div></div>
    );
}

export default Albums;
